apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-learncicd-worker
  labels:
    {{- include "learncicd-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "learncicd-worker.selectorLabels" . | nindent 4 }}
